# ── Go server ──────────────────────────────────────────
server/vendor/
server/*.exe
server/*.test
server/*.out
server/heartchat-server

# ── Runtime data (per-user state, do not commit) ────────
server/data/

# ── Environment secrets (never commit) ──────────────────
.env
*.env

# ── IDE / editor artifacts ──────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# ── WeChat Developer Tools caches ───────────────────────
miniprogram/.cache/
miniprogram/node_modules/
